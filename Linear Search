#include <iostream>

using namespace std;
bool check(int a[],int n,int key){
    for(int i=0;i<n;i++){
        if(a[i]==key)
        return 1;
    }
    
    return 0;
}

int main()
{ int arr[]={1,2,3,4,5,6,7,8,8,1};
  bool k= check(arr,10,9);
  if(k){
      cout<<"yes";
  }
  else
  {cout<<"no";}
    

    return 0;
}
